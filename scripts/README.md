# Scripts

Use this directory for thin, reusable project orchestration that does not belong in
an importable Python module. Research logic should normally live under `tools/` and
run through `./invenv.sh`.
